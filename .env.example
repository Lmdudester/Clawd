# Required for persistent login sessions across server restarts.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# JWT_SECRET=

# Optional: Git identity for commits made inside Docker sessions
# GIT_USER_NAME=Your Name
# GIT_USER_EMAIL=you@example.com

# Optional: GitHub PAT for git push from Docker sessions (use one, not both)
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
# GIT_CREDENTIALS_URL=https://username:token@github.com

# Optional: Host volume mount for sharing files with session containers.
# On Windows (Docker Desktop with WSL): HOST_VOLUME_MOUNT=/c  HOST_VOLUME_TARGET=/host/c
# On Linux/macOS:                       HOST_VOLUME_MOUNT=/home  HOST_VOLUME_TARGET=/host/home
# HOST_VOLUME_MOUNT=/c
# HOST_VOLUME_TARGET=/host/c
# HOST_DRIVE_PREFIX=/host/c

# Optional: Container path to host ~/.claude dir (enables global skills, settings, CLAUDE.md)
# HOST_CLAUDE_DIR=/host/c/Users/YourUsername/.claude

# Optional: Push notifications via ntfy.sh (install ntfy app and subscribe to your topic)
# NTFY_TOPIC=clawd-your-topic
# NTFY_SERVER=https://ntfy.sh
