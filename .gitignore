node_modules/
dist/
.env
credentials.json
claude-auth.json
*.log
.DS_Store

# User-specific local data
project-folders.json
project-repos.json
.claude/settings.local.json
.claude/plans/

# Build artifacts
*.tsbuildinfo

# System artifacts
*.stackdump

# Root copy of logo (canonical copy is in packages/client/public/)
/clawd.png

# Git credentials (written by entrypoint inside Docker)
.git-credentials

# User docker-compose overrides (example files are tracked)
docker-compose.autoupdate.yml
